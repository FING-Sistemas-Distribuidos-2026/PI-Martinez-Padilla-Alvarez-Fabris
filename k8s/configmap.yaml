apiVersion: v1
kind: ConfigMap
metadata:
  name: raytracer-config
  namespace: raytracer
data:
  PORT: "8000"
  POSTGRES_DB: raytracer
  RABBITMQ_QUEUE: render_jobs
  MINIO_ENDPOINT: minio-service:9000
  MINIO_BUCKET: renders
  API_BASE_URL: http://web-service:8000
